apiVersion: v1
kind: Service
metadata:
  name: bus-passenger-ui
spec:
  type: NodePort
  selector:
    app: bus-passenger-ui
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
